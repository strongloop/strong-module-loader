{
  "name": "strong-module-loader",
  "description": "separate your app into modules loaded by config files",
  "version": "0.0.4",
  "scripts": { 
    "blanket": {
      "pattern": "//^((?!(node_modules|test)).)*$/"
    },
    "coverage": "mocha -r blanket -R html-cov > coverage_strong-module-loader.html",
    "test": "mocha"
  },
  "license": "MIT",
  "dependencies": {
    "debug": "latest",
    "strong-config-loader": "~0.0.1"
  },
  "devDependencies": {
    "blanket": "latest",
    "mocha": "latest"
  },
  "repository" : {
    "type" : "git",
    "url" : "http://github.com/strongloop/sl-module-loader.git"
  }
}
